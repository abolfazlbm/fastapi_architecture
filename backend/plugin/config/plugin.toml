[plugin]
summary = "Parameter configuration"
version = "0.0.2"
description = "Usually used to dynamically configure system parameters and display front-end engineering data"
author = "wu-clan"
tags = ["other"]
database = ["mysql", "postgresql"]

[app]
extend = "admin"

[api.config]
prefix = "/configs"
tags = "System parameter configuration"